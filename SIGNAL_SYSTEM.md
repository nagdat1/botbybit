# 📡 نظام الإشارات - دليل شامل

## 🎯 نظرة عامة

البوت يدعم الآن نظاماً احترافياً لاستقبال وتنفيذ إشارات التداول من TradingView على الحسابات الحقيقية (Bybit & MEXC).

---

## 📊 أنواع الإشارات المدعومة

### 1️⃣ إشارات SPOT (التداول الفوري)

| الإشارة | الإجراء | الوصف |
|---------|---------|-------|
| `action: "buy"` | فتح صفقة شراء | شراء العملة بالسعر الحالي |
| `action: "sell"` | إغلاق الصفقة | بيع العملة وإغلاق الصفقة |

**مثال - شراء Spot:**
```json
{
  "symbol": "BTCUSDT",
  "action": "buy"
}
```

**مثال - بيع Spot:**
```json
{
  "symbol": "BTCUSDT",
  "action": "sell"
}
```

---

### 2️⃣ إشارات FUTURES (العقود الآجلة)

#### LONG (صفقات الشراء)

| الإشارة | الإجراء | الوصف |
|---------|---------|-------|
| `type: "LONG"`, `action: "open"` | فتح صفقة Long | فتح صفقة شراء بالرافعة المالية |
| `type: "LONG"`, `action: "close"` | إغلاق صفقة Long | إغلاق صفقة الشراء |

**مثال - فتح Long:**
```json
{
  "symbol": "BTCUSDT",
  "type": "LONG",
  "action": "open",
  "take_profit": 52000,
  "stop_loss": 48000
}
```

**مثال - إغلاق Long:**
```json
{
  "symbol": "BTCUSDT",
  "type": "LONG",
  "action": "close"
}
```

#### SHORT (صفقات البيع)

| الإشارة | الإجراء | الوصف |
|---------|---------|-------|
| `type: "SHORT"`, `action: "open"` | فتح صفقة Short | فتح صفقة بيع بالرافعة المالية |
| `type: "SHORT"`, `action: "close"` | إغلاق صفقة Short | إغلاق صفقة البيع |

**مثال - فتح Short:**
```json
{
  "symbol": "BTCUSDT",
  "type": "SHORT",
  "action": "open",
  "take_profit": 48000,
  "stop_loss": 52000
}
```

**مثال - إغلاق Short:**
```json
{
  "symbol": "BTCUSDT",
  "type": "SHORT",
  "action": "close"
}
```

---

## ⚙️ إعداد TradingView

### 1. إنشاء Alert (تنبيه)

1. افتح الرسم البياني في TradingView
2. اضغط على أيقونة التنبيه (🔔)
3. اختر الشرط المناسب
4. في قسم "Webhook URL"، أدخل رابط البوت الخاص بك

### 2. رابط Webhook الشخصي

```
https://your-domain.com/personal/{user_id}/webhook
```

**مثال:**
```
https://botbybit.railway.app/personal/123456789/webhook
```

### 3. رسالة Webhook

#### لإشارة Spot BUY:
```json
{
  "symbol": "{{ticker}}",
  "action": "buy"
}
```

#### لإشارة Spot SELL:
```json
{
  "symbol": "{{ticker}}",
  "action": "sell"
}
```

#### لإشارة LONG Futures:
```json
{
  "symbol": "{{ticker}}",
  "type": "LONG",
  "action": "open",
  "take_profit": 52000,
  "stop_loss": 48000
}
```

#### لإشارة إغلاق LONG:
```json
{
  "symbol": "{{ticker}}",
  "type": "LONG",
  "action": "close"
}
```

---

## 🔧 كيفية عمل النظام

### 1. استقبال الإشارة
- يستقبل البوت الإشارة من TradingView عبر Webhook
- يتحقق من نوع الحساب (حقيقي/تجريبي)
- يحدد المنصة المفعّلة (Bybit/MEXC)

### 2. معالجة الإشارة
- يحلل نوع الإشارة (SPOT/LONG/SHORT)
- يحدد الإجراء (open/close/buy/sell)
- يحسب الكمية بناءً على:
  - مبلغ التداول المحدد
  - الرافعة المالية (للفيوتشر)
  - السعر الحالي

### 3. تنفيذ الأمر
- يرسل الأمر مباشرة إلى المنصة (Bybit/MEXC)
- يطبق TP/SL إذا كانت محددة
- يسجل النتيجة ويرسل إشعار

---

## 💰 حساب الكمية

### SPOT:
```
الكمية = مبلغ التداول ÷ السعر الحالي
```

**مثال:**
- مبلغ التداول: $100
- سعر BTC: $50,000
- الكمية: 100 ÷ 50,000 = 0.002 BTC

### FUTURES:
```
الكمية = (مبلغ التداول × الرافعة المالية) ÷ السعر الحالي
```

**مثال:**
- مبلغ التداول: $100
- الرافعة: 10x
- سعر BTC: $50,000
- الكمية: (100 × 10) ÷ 50,000 = 0.02 BTC

---

## 🎯 Take Profit & Stop Loss

### تعيين TP/SL عند فتح الصفقة:
```json
{
  "symbol": "BTCUSDT",
  "type": "LONG",
  "action": "open",
  "take_profit": 52000,
  "stop_loss": 48000
}
```

### تعيين TP/SL لصفقة مفتوحة:
1. اذهب إلى "🔄 الصفقات المفتوحة"
2. اختر الصفقة
3. اضغط "🎯 تعيين TP" أو "🛡️ تعيين SL"
4. أدخل السعر المطلوب

---

## 🔐 الحسابات الحقيقية

### ربط API:
1. اذهب إلى "⚙️ الإعدادات"
2. اضغط "🏦 اختيار المنصة"
3. اختر Bybit أو MEXC
4. اضغط "🔗 ربط API"
5. أدخل API Key و Secret
6. اضغط "✅ استخدام المنصة"

### عرض البيانات الحقيقية:
- **المحفظة**: تعرض الرصيد الفعلي من المنصة
- **الصفقات المفتوحة**: تعرض الصفقات الحقيقية
- **تاريخ التداول**: يعرض الأوامر المنفذة
- **الإعدادات**: تعرض حالة الربط والرصيد

---

## 📝 ملاحظات هامة

### ✅ ما يدعمه النظام:
- ✅ Spot BUY/SELL على Bybit و MEXC
- ✅ Futures LONG/SHORT على Bybit فقط
- ✅ Take Profit & Stop Loss
- ✅ حساب الكمية تلقائياً
- ✅ تنفيذ فوري على المنصة

### ❌ ما لا يدعمه النظام:
- ❌ SHORT على SPOT (غير منطقي)
- ❌ Futures على MEXC (غير مدعوم من المنصة)
- ❌ أوامر Limit (فقط Market Orders)

---

## 🚀 أمثلة عملية

### سيناريو 1: تداول Spot على Bybit
```json
// إشارة شراء
{
  "symbol": "ETHUSDT",
  "action": "buy"
}

// بعد فترة... إشارة بيع
{
  "symbol": "ETHUSDT",
  "action": "sell"
}
```

### سيناريو 2: تداول Futures Long
```json
// فتح Long
{
  "symbol": "BTCUSDT",
  "type": "LONG",
  "action": "open",
  "take_profit": 55000,
  "stop_loss": 49000
}

// إغلاق Long
{
  "symbol": "BTCUSDT",
  "type": "LONG",
  "action": "close"
}
```

### سيناريو 3: تداول Futures Short
```json
// فتح Short
{
  "symbol": "BTCUSDT",
  "type": "SHORT",
  "action": "open",
  "take_profit": 48000,
  "stop_loss": 52000
}

// إغلاق Short
{
  "symbol": "BTCUSDT",
  "type": "SHORT",
  "action": "close"
}
```

---

## 🔍 استكشاف الأخطاء

### الإشارة لا تُنفذ:
1. تحقق من ربط API
2. تحقق من تفعيل الحساب الحقيقي
3. تحقق من صيغة الإشارة
4. تحقق من الرصيد المتاح

### رسالة "No position found":
- تأكد من وجود صفقة مفتوحة قبل محاولة الإغلاق
- تحقق من الرمز (Symbol) الصحيح

### رسالة "Invalid signal format":
- تحقق من صيغة JSON
- تأكد من وجود الحقول المطلوبة
- راجع الأمثلة أعلاه

---

## 📞 الدعم

للمزيد من المساعدة:
- راجع logs البوت
- تحقق من حالة API في الإعدادات
- اختبر الاتصال بالمنصة

---

**تم التحديث:** 2025-01-13
**الإصدار:** 2.0 - نظام الإشارات الاحترافي

